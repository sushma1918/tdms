
pipeline {
   agent { label 'master' }
   stages {
         stage('GET CSV FILE') {
             steps {
                 script {
                   echo "Hello"
                 }
              }
             
         }
         
   }
    
}
       

